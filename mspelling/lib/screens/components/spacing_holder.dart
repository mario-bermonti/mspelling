import 'package:flutter/material.dart';

class SpacingHolder extends StatelessWidget {
  const SpacingHolder({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 10);
  }
}
